#include "main.h"
#include <stdio.h>

/**
 * main - Entry point of the function
 * _puts - a function that writes a line to standard output
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
